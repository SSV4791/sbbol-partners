post:
  operationId: getRenter
  summary: Получение арендатора
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: model/renterIdentifier.json
  responses:
    '200':
      description: Successful operation
      content:
        application/json:
          schema:
            $ref: model/renter.json
            type: object
