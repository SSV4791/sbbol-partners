apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: egress-appjournal-gw-5
spec:
  selector:
    istio: egressgateway-${NAMESPACE}
  servers:
    - hosts:
        - ${APPJOURNAL_HOST_5}
      port:
        name: tcp-${APPJOURNAL_PORT_LISTENER_5}
        number: ${APPJOURNAL_PORT_LISTENER_5}
        protocol: TCP
