apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: vs-egress-db-si
spec:
  exportTo:
    - .
  gateways:
    - egress-db-si-gw
    - mesh
  hosts:
    - ${DB_SI_HOST}
  tcp:
    - match:
        - gateways:
            - mesh
          port: ${DB_SI_PORT_PROXY}
      route:
        - destination:
            host: egressgateway-svc
            port:
              number: ${DB_SI_PORT_LESTENER}
          weight: 100
    - match:
        - gateways:
            - egress-db-si-gw
          port: ${DB_SI_PORT_LESTENER}
      route:
        - destination:
            host: ${DB_SI_HOST}
            port:
              number: ${DB_SI_PORT}
          weight: 100
