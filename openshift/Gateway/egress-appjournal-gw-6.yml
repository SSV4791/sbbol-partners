apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: egress-appjournal-gw-6
spec:
  selector:
    istio: egressgateway-${NAMESPACE}
  servers:
    - hosts:
        - ${APPJOURNAL_HOST_6}
      port:
        name: tcp-${APPJOURNAL_PORT_LISTENER_6}
        number: ${APPJOURNAL_PORT_LISTENER_6}
        protocol: TCP
