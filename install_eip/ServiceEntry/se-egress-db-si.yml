apiVersion: networking.istio.io/v1alpha3
kind: ServiceEntry
metadata:
  name: se-egress-db-si
spec:
  exportTo:
    - .
  hosts:
    - ${SI_DB_HOST}
  location: MESH_EXTERNAL
  ports:
    - name: tcp
      number: ${DB_PORT}
      protocol: TCP